import React from 'react'

class App extends React.Component {
  constructor (props) {
    super (props);
  }

  render () {
    return (
      <div id = "App"> hello! this is from the App with CSS working</div>
    )
  }
}

export default App